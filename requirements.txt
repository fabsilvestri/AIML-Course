# Everything tools/*.py needs. Versions are the ones the figures were built
# with; anything newer should be fine, but a figure that changes is a bug worth
# investigating rather than accepting.
#
#   python3 -m pip install -r requirements.txt

numpy>=2.0
pandas>=2.0
matplotlib>=3.10
scikit-learn>=1.4          # root_mean_squared_error arrived in 1.4
scipy>=1.16
statsmodels>=0.14          # Lectures 15-16, autocorrelation and differencing

# Lectures 10-18 and 20-24
torch>=2.9                 # MPS on Apple Silicon; no CUDA needed for the course
torchvision>=0.28          # FashionMNIST, CIFAR10, Flowers102, pretrained weights
transformers>=4.57         # Lectures 17-18 and 20-24

# Building the artefacts
nbformat>=5.10
nbclient>=0.10             # tools/make_notebooks.py --check executes the notebooks
fonttools>=4.0             # regenerating the static font instances
brotli                     # woff2 compression for those fonts
